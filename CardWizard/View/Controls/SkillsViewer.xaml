<UserControl x:Class="CardWizard.View.SkillsViewer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CardWizard.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="916">
    <!--技能面板-->
    <Grid x:Name="MainGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="42"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Label Content="INVESTIGATOR SKILLS" Tag="Investigator.Skills.Title"
               FontSize="22" Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="99"
               Foreground="{DynamicResource ButtonForeground}"
               Style="{DynamicResource XLabel}" Background="{DynamicResource SkillBorderBrush}"/>
        <WrapPanel x:Name="Container" 
                   Orientation="Vertical" 
                   Grid.Column="0" Grid.Row="1" Grid.ColumnSpan="99" Margin="2,2,2,2"
                   ItemWidth="226" ItemHeight="48">
        </WrapPanel>
    </Grid>
</UserControl>
